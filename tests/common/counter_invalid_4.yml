version: "2.0"
sample:
    name: counter_invalid_4
method:
  - component_tag: "counter"
    technique_name: "count"
    max_duration: 0.2
    sampling_interval: 0.1
    task_params:
        unknown: bla
settings:
    unlock_when_done: true
    verbosity: DEBUG